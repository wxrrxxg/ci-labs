function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}

module.exports = {
    add,
    subtract
};

function multiply(a, b) {
    return a * b;
}

// Testing notify condition on pull request